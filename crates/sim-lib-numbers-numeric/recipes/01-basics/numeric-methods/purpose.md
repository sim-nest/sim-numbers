# Numeric methods (descriptor)

The `numbers/numeric` methods -- numeric differentiation, integration, adaptive
integration, and ODE solving -- are iterative solvers that run outside the sandbox eval
stack. This recipe documents the method surface; the `numbers/quad` and `numbers/rk`
recipes document the underlying quadrature and Runge-Kutta engines.
