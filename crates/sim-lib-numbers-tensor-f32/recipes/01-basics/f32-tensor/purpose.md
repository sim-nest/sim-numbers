# F32 tensor descriptor

This recipe exposes the `f32` tensor descriptor installed by the single-precision backend. Runtime code can discover it as an ordinary typed tensor component and use explicit tensor casts to enter or leave this storage domain.
