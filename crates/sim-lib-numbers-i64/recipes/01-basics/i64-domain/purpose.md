# Add 64-bit integers

Add two `i64` literals through the shared `math/add` entry point. The integer
domain computes `8 + 13 = 21` -- a real value evaluated through the runtime, not
a quoted feature list.
