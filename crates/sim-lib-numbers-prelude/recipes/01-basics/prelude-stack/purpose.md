# Numbers prelude stack (descriptor)

This records the `numbers/prelude` load order: the deterministic stack of number-domain
libraries loaded together to form the standard numeric environment. It documents a load
manifest, not an expression with a computed value.
