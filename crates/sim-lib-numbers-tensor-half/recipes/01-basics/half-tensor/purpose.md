# Half tensor descriptors

This recipe exposes the `f16` tensor descriptor installed by the half backend. The same library also installs the `bf16` descriptor, so runtime discovery can choose either compact floating-point storage target.
