# Cast a tensor explicitly

This recipe casts an f64 tensor into the f32 tensor dtype. Conversion is an explicit `tensor/cast` operation, so narrowing, rounding, special values, and unsupported domains are handled at the cast boundary.
