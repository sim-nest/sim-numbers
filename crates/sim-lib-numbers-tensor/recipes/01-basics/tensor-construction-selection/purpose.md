# Construct and select

Build a finite inclusive grid through the canonical Tensor executor operation,
then select its nonzero cells in stable row-major order. The endpoint policy is
part of the request rather than an ambient numerical default.
