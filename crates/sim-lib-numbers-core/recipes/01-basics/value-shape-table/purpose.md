# Number value/shape table (descriptor)

This records the `numbers/core` value/shape table -- the mapping from number-domain
values to their kernel `Shape` descriptors. It documents runtime metadata, not an
expression that reduces to a value, so there is nothing to compute live.
