# Certified decompositions

Load the decomposition library and request QR, symmetric eigen, real Schur, or
SVD with explicit limits. Inspect completion, rank, permutation, reconstruction,
orthogonality, and provider identity evidence before consuming factors. Use one
declared singular cutoff for every SVD-derived solve.
