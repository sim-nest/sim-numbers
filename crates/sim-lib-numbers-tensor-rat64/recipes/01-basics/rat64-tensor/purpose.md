# Construct a rational-cell tensor

Build a length-2 tensor typed over the `numbers/rational` element domain, with exact
fraction cells `1/2` and `2/3`. Read-construct builds the shaped, exactly-typed value
directly -- a live tensor of rationals, not a quoted descriptor.
