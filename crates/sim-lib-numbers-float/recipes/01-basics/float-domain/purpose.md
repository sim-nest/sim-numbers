# Float domain (descriptor)

The `numbers/float` domain covers the `f32` and `f64` finite-literal families. Its live
arithmetic is demonstrated by the runnable `numbers/f64` add-floats recipe (`math/add
1.5 2.0 -> 3.5`); this recipe documents the broader float-domain literal surface.
