[package]
name = "sim-lib-numbers-tensor-bit"
version = "0.3.0"
edition = "2024"
license = "MPL-2.0"
description = "Packed boolean tensor specialization with bitwise tensor operations."
readme = "README.md"
publish = true
repository = "https://github.com/sim-nest/sim-numbers"
homepage = "https://github.com/sim-nest/sim-numbers"

[dependencies]
sim-cookbook = { version = "0.2.0" }
sim-kernel = { version = "0.2.0" }
sim-lib-numbers-tensor = { version = "0.2.1", path = "../sim-lib-numbers-tensor" }

[build-dependencies]
sim-cookbook = { version = "0.2.0" }
