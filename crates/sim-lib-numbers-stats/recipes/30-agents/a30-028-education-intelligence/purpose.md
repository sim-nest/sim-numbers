# Education-intelligence trace (descriptor)

A deterministic, offline synthetic trace of an education-intelligence agent: IRT placement,
Bayesian knowledge tracing, and SM-2 spacing over a synthetic learner panel. It demonstrates
the pipeline shape; its statistical/ML operators run outside the sandbox eval stack, so the
trace is documented rather than executed live.
