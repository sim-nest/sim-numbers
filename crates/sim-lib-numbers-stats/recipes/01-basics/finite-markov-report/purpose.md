# Finite Markov Report (descriptor)

The generic statistics owner fits a first-order model from finite state
sequences. The report exposes exact transition counts, additive smoothing,
training and held-out likelihood, corpus identity/license/hash, and a
deterministic text representation. Domain adapters supply their own ordered
state type and stable label; the numbers crate contains no domain-specific
states.
