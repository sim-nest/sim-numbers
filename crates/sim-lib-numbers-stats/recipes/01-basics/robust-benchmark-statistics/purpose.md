# Robust benchmark statistics

Use the statistics owner's raw median absolute deviation to describe benchmark
dispersion, then compute a reproducible candidate-minus-baseline mean interval.
The caller supplies both the resampling seed and a hard work allowance.
