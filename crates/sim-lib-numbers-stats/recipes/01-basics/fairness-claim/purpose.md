# Fairness claim (descriptor)

This records a fairness claim -- a structured assertion over group-outcome statistics.
Checking it requires the statistical operators (ratios, disparate-impact) the sandbox eval
stack does not provide, so it is documented as a descriptor. The `numbers/stats`
stats-helpers recipe names the underlying helper surface.
