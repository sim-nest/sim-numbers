# Numeric plugin descriptor

This records the codec-neutral numeric-plugin descriptor: the metadata by which a number
domain advertises itself to the runtime. It names a descriptor record, not an expression
with a single computed value, so it is documented rather than evaluated.
