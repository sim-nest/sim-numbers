# Inspect reusable sampled-data algorithms

Round-trip an odd multilevel signal through the Le Gall 5/3 wavelet, evaluate a
physically scaled Savitzky-Golay second derivative, solve a Toeplitz system with
residual evidence, and resolve duplicate interpolation coordinates explicitly.
The checked output keeps each policy outcome visible rather than relying on a
domain-specific signal convention.
