# Inspect convolution selection

This specimen uses the public `signal/convolve` form with explicit mode and
algorithm policy. The result keeps the samples beside the direct/FFT cost
comparison and retained span, so automatic selection is reviewable rather than
an ambient performance guess.
