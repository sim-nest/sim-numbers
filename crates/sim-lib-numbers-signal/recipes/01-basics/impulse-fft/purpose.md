# Impulse FFT

The impulse has equal energy in every Fourier bin. This checked Lisp specimen
runs the loadable signal transform with explicit direction, normalization,
packing, and length conventions.
