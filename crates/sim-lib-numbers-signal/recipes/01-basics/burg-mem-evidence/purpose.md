# Select an autoregressive order and inspect its MEM peak

The deterministic generated AR(2) fixture fits every candidate through order
eight, selects by BIC, and retains residual, reflection-margin, effective-order,
and bounded MEM-spectrum work evidence.
