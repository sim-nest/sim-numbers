# Reconstruct a periodogram's scale

Estimate a bin-centered tone through a periodic Hann window. The checked output
retains coherent gain, equivalent noise bandwidth, degrees of freedom, and the
admitted work ceiling beside the peak, so the reported power is reproducible
from public evidence rather than an ambient FFT convention.
