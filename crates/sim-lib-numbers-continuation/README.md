# sim-lib-numbers-continuation

Bounded, restartable pseudo-arclength continuation for one-dimensional residual
manifolds. The API preserves seed admission, prediction rejection, bordered
Newton, rank, tangent, fold, orientation, step adaptation, and termination
evidence. Parameter-domain exits are reported and never projected or clamped.
