# Tensor linear algebra (descriptor)

Dot product, matrix multiply, and determinant are tensor operations the sandbox eval stack
does not execute (it constructs tensors but does not operate on them). This recipe documents
the `numbers/tensor-linalg` surface; the runnable tensor-construction recipes show the live
construction side.
