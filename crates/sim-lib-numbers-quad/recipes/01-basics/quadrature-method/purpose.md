# Quadrature methods (descriptor)

Numerical quadrature (Simpson, adaptive) approximates a definite integral by sampling and
can also integrate strictly monotone scalar or vector samples with explicit
orientation, unmatched-interval, and compensated-sum evidence.
summing. The adaptive iteration runs outside the sandbox eval stack, so this recipe
documents the `numbers/quad` method surface rather than integrating live.
