# sim-lib-numbers-optimize

Evidence-carrying bounded scalar and multivariate optimization, nonlinear and
linear least squares. Every plan declares bounds, scaling, derivative policy,
tolerances, and resource limits. The optional `assignment` feature is only a
numeric conversion adapter to the certified owner in `sim-lib-discrete-graph`.
