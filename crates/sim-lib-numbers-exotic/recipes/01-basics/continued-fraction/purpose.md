# Continued fractions (descriptor)

A continued-fraction expansion iterates a recurrence to approximate a real (here the
prefix of pi) as a nested fraction. The iteration runs outside the sandbox eval stack,
so this recipe documents the `numbers/exotic` continued-fraction / `as-f64` interface
rather than expanding it live.
