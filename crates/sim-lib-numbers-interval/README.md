# sim-lib-numbers-interval

Sealed mathematical enclosures with directed binary64 and exact rational
kernels. Estimates are a separate type and can never be promoted into a
certificate. Certified values retain their reviewed issuer, exact input
identity, operation trace digest, and method evidence.
