# Release automation keeps workspace crate versions synchronized and publishes
# crates in dependency order.
[workspace]
# Workspace crate versions move together.
dependencies_update = true
git_release_enable = true
# release-plz computes the publish order from crate dependencies.
